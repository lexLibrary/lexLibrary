version: '3'
services:
  webdriver:
    image: selenium/standalone-chrome:3.0
    logging:
        driver: "none"
    volumes:
      - /dev/shm:/dev/shm
  tests:
    build: "../build"
    ports:
        - "8080"
    links:
      - webdriver
    volumes:
      - ../..:/go/src/github.com/lexLibrary/lexLibrary
    environment:
      LLTEST: 'true'
      LLDATABASE: 'sqlite'
      LLPORT: 8080
      LLHOST: "tests"
      LLWEBDRIVERURL: "http://webdriver:4444/wd/hub"
      LLDEBUGONERR: "true"
      GOPATH: /go
      LLBROWSER: 'chrome'
    command: sh -c '
        cd /go/src/github.com/lexLibrary/lexLibrary/ && go test ./web
      '
